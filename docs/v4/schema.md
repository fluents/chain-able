


```ts
export const is = {
	isArray,
	notEmptyArray,
	isString,
	isNumber,
	isFunction,
	isObj,
	isObjWithKeys,
	isEnumerable,
	isError,
	isMap,
	isSet,
	isIterator,
	isDate,
	isRegExp,
	isPureObj,
	isSymbol,
	isReal,
	isBoolean,
  isNull,
	isUndefined,
  toS,

  // only available as `chain-able/deps/is/*` without the `is`
  // @example `chain-able/deps/is/stringOrNumber`, `chain-able/deps/is/true`
	isStringOrNumber,
	isNullOrUndef,
	isTrue,
	isFalse,
	isDot,
	isMapish,
}
```
