/* istanbul ignore next */

const map = require('./map')
const set = require('./set')
const regexp = require('./regexp')

/**
 * @member construct
 * @type {Object}
 */
module.exports = {
  map,
  set,
  regexp,
}
